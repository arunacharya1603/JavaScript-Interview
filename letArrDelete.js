let a = [1,2,3,4,5,6,7];
//This line declares a variable a and initializes it with an array containing the elements [1, 2, 3, 4, 5, 6, 7].
console.log(delete a);
console.log(delete a[1]);


// The delete operator is used to remove a property from an object.
//false ---> as delete is used to remove the property of an object it will not affect the variables declared with let const and var.